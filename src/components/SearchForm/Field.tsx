import styled from 'styled-components'

const Field = styled.div`
  display: inline-flex;
  align-items: center;
  gap: 8px;
  min-width: 150px;
  white-space: nowrap;

  label {
    margin: 0;
  }
`

export default Field
